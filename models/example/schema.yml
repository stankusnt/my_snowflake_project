
version: 2

models:
  # - name: my_first_dbt_model
  #   description: "A starter dbt model"
  #   columns:
  #     - name: id
  #       description: "The primary key for this table"
  #       tests:
  #         - unique
  #         - not_null

  # - name: my_second_dbt_model
  #   description: "A starter dbt model"
  #   columns:
  #     - name: id
  #       description: "The primary key for this table"
  #       tests:
  #         - unique
  #         - not_null

  - name: first_computer_software
    description: "TYPICAL MODEL"
    columns:
      - name: ID
        description: "The primary key for this table"
        tests:
          - unique
          - not_null

  - name: founded_2015
    description: "TYPICAL MODEL"
    columns:
      - name: ID
        description: "The primary key for this table"
        tests:
          - unique
          - not_null